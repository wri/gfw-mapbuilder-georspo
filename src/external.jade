doctype
html
  body.tundra
    include jade/svg.jade
    #root
    div#share-modal.modal-wrapper.hidden
    script(id='library-load' src='http://alpha.blueraster.io.s3.amazonaws.com/gfw-mapbuilder/library-load-dist/1.1.10/js/library.js')
    
    script.
      var ll = new MapBuilder({
        el: 'root',
        config: {
          language: 'en',
          title: 'Lucas Fake Application',
          labels: {
            en: {
              title: 'New TITLE for our Application from our config',
              subtitle: 'New subbb'
            }
          }
        },
        version: "#{version}"//,
        //base: "#{base}"
      });
